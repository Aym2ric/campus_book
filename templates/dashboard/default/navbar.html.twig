<nav class="navbar col-lg-12 col-12 p-0 fixed-top d-flex flex-row">
    <div class="text-center navbar-brand-wrapper d-flex align-items-center justify-content-center">
        <!--<a class="navbar-brand brand-logo" href="{{ path('dashboard_index') }}"><img style="width:100px; height: 100px;" src="{{ asset('assets/images/logo.png') }}" alt="logo"/></a>-->
        <!--<a class="navbar-brand brand-logo-mini" href="{{ path('dashboard_index') }}"><img src="{{ asset('assets/images/logo.png') }}" alt="logo"/></a>-->
    </div>
    <div class="navbar-menu-wrapper d-flex align-items-center justify-content-end">
        <button class="navbar-toggler navbar-toggler align-self-center" type="button" data-toggle="minimize">
            <span class="fa fa-bars"></span>
        </button>

        <ul class="navbar-nav navbar-nav-right">
            <li class="nav-item nav-profile dropdown">
                <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown" id="profileDropdown" aria-expanded="false">
                        <span class="w-32">
                            <img src={{ asset('assets/images/50x50.png')}}>
                        </span>
                        <span class="ml-2 text-black">{{ app.user.username }}</span>
                    </a>
                <div class="dropdown-menu dropdown-menu-right navbar-dropdown" aria-labelledby="profileDropdown">
                     <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('dashboard_password') }}">
                        <i class="fa fa-user text-primary"></i> Mon compte
                       
                    </a>

                    {% if is_granted('ROLE_SUPER_ADMIN') %}
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('admin_index') }}">
                            <i class="fa fa-cog text-primary"></i> Switch vers Admin
                        </a>
                    {% endif %}
                    
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">
                        <i class="fa fa-sign-out text-primary"></i> Déconnexion
                    </a>
                    <div class="dropdown-divider"></div>

                </div>
            </li>
        </ul>
        <button class="navbar-toggler navbar-toggler-right d-lg-none align-self-center" type="button" data-toggle="offcanvas">
            <span class="fa fa-bars"></span>
        </button>
    </div>
</nav>
